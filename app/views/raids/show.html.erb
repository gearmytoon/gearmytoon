<%= render "shared/character_info", :character => @character %>

<% render :layout => 'shared/upgrade_section', :locals => {:kind_of_upgrade => "raid", :upgrades => @character.top_upgrades_for(@raid.items_dropped_in, false)} do %>
  <%= @raid.name %>
<% end %>
